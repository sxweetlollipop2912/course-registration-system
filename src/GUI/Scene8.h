#pragma once

#include "Interaction.h"

void scene8(sf::RenderWindow &window, App &_app);

void modifyScoreScene(sf::RenderWindow &window, App &_app);

void importScoreScene(sf::RenderWindow &window, App &_app);