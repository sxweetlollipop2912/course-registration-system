#pragma once

#include <string>

using std::string;

namespace Const {
    const string DEFAULT_PASS = "1234";

    /// For debugging
    const string ADMIN_USERNAME = "admin";
    const string ADMIN_PASS = "admin";
}